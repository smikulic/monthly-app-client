import { styled } from "@mui/material/styles";

export const CategoryDetailsStyled = styled("div")({
  display: "flex",
  alignItems: "center",
});

export const CategoriesActionsBarStyled = styled("div")({
  display: "flex",
  justifyContent: "flex-end",
  alignItems: "center",
  padding: "0 32px",
  height: "56px",
  fontSize: "18px",
});

export const CategoryAmountStyled = styled("div")({
  fontSize: "16px",
  color: "#eec22f",
});
